serenity {
  take.screenshots = FOR_FAILURES
  browser = chrome
  webdriver
  {
    driver = chrome
  }
  base.url = "https://magento.softwaretestingboard.com"
  timeouts {
      implicitlywait = 5000
      fluentwait = 10000
  }
}
